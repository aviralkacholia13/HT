# Infrastructure Notes

Infrastructure manifests for Lablens will live under this directory.

- `nginx/` – Reverse proxy configuration for local development and production hardening.
- `migrations/` – Alembic-generated SQL migrations for the PostgreSQL schema defined in the design blueprint.

Future additions will include Docker Compose definitions, GitHub Actions workflows, and deployment scripts aligning with the MVP timeline.
