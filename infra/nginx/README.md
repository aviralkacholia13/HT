# Nginx Configuration Placeholder

This folder will contain the reverse proxy configuration used to terminate TLS and forward traffic to the FastAPI and Next.js services. Final configuration will enforce HTTPS, strict security headers, and gzip/brotli compression as specified in `docs/design.md`.
